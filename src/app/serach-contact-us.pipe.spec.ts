import { SerachContactUsPipe } from './serach-contact-us.pipe';

describe('SerachContactUsPipe', () => {
  it('create an instance', () => {
    const pipe = new SerachContactUsPipe();
    expect(pipe).toBeTruthy();
  });
});
